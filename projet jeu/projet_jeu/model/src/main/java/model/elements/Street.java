package model.elements;

/**
 * La classe Street
 */
public class Street extends Element {
    /**
     * Constructeur de la classe
     * @param x
     * Position X
     * @param y
     * Position Y
     * @param image
     * Le chemin de l'image de la route
     */
    public Street(int x, int y, String image) {
        super(x, y, image);
    }
}
